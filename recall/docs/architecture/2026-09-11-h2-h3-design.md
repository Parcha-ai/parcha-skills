I have what I need. Here is the plan.

# Recall rewrite — HILL 2 (retrieval quality) and HILL 3 (S3 search plane)

Repo: `/home/ubuntu/worktrees/recall-rewrite-20260911/recall`. Line numbers reference the current tree.

## Ground truth from the code (things the plan depends on)

- Fusion is RRF at `server/recall_server/passage_retrieval.py:137` (`value["_score"] += weight / (60 + rank)`), weights hard-coded at `:1049-1053`. Arm rows carry `score` (`1-distance` for dense, `ts_rank_cd` for text arms, `0.0::real` in the recent-first fallback at `:390,:575`) — so min-max needs a per-arm "no score" guard.
- Candidate depth is `candidate_limit = min(400, max(80, limit*20))` at `:997`; limit is capped at 20 in three places: `mcp.py:827`, `canonical_retrieval.py:1722` (`passage_hints`), and `search_bundle` allows 100 (`passage_retrieval.py:1118`).
- Embedding reuse key is `content_sha256 = passage.text_sha256` (`passage_index.py:516-517`, `:754`), gated by `runtime.passage_fingerprint` (`semantic.py:312-324`), which hashes `PASSAGE_EMBEDDING_CONTRACT` (`semantic.py:29`). Changing `PassagePolicy.fingerprint` would re-id every passage (`passage_projection.py:384-408`) and force a full re-projection from S3 evidence parts — avoid.
- A contextual-header renderer already exists: `passage_representations.py:181-231` `_metadata_lines(DocumentContext)` (source family, aliases, people with relations/aliases, harness, workspace basename, branch, document start/end) and the catalog query that feeds it, `_context_rows` (`:409-565`). It is only used by the shadow representation index. H2-a reuses the renderer and the query, not the support-passage machinery.
- Sparse-exact arm scans `canonical_chunks` (`passage_retrieval.py:583-596`); liveness for the other arms is also a `LEFT JOIN canonical_chunks live_chunk` (`:927-933`), and `show`/`session_context`/`related`/`_receipt_event`/time clip all read `canonical_chunks` (`canonical_retrieval.py:1887, 2881-2904, 2991, 3030, 4014, 4061`).
- Truth scoring: `evals/agentic_truth.py:635-685` computes recall@20, recall@50, MRR over the first 20; `evals/systems_card/accuracy.py:23` pins `CANDIDATE_LIMIT = 20`, gates at `:132-137` (recall@20 ≥ 0.6, MRR ≥ 0.3, p95 ≤ 5 s). Server latency gate at `latency.py:222-225`.
- Parquet passages dataset schema at `parquet_scan.py:240-259` already has nearly the Lance column set (missing `vector`, `header`, `content_sha256`, `spans`). Queue semantics with generations: `canonical_parquet_scan_queue` (`server/schema/050_parquet_scan_plane.sql`), fed from `passage_index.py:549-574` on every passage commit, with `reason='forget'` allowed.
- Deps: `server/requirements.txt` has `pyarrow==25.0.1`, `boto3`; no `lance`/`duckdb`.

---

## HILL 2 — Postgres-resident retrieval quality

Order: H2-d → H2-b → H2-c → H2-a → H2-e. (H2-d is a prerequisite for measuring recall@50 and for the reranker's 50-candidate input; H2-a is last because it needs a one-time re-embed and its gain is measured against the already-improved pipeline.)

### H2-d Depth 50, `candidate_depth`, exact arm over `canonical_passages`

Files / changes:
- `mcp.py:122-127,178-183` (schema `maximum`) and `:827` → 50. `canonical_retrieval.py:1722` → `1 <= limit <= 50`; `:831` (`_legacy_chunk_search_for_eval`) can stay at 20.
- `passage_retrieval.py:997`: keep `candidate_limit` formula but expose it: add `"candidate_depth": candidate_limit` and `"result_limit": limit` to `diagnostics` (`:1057-1080`). `DENSE_NEAREST_LIMIT` (`:53`) stays 400; at limit 50 `candidate_limit` is already 400 so cost does not change.
- Sparse arm: rewrite `_sparse_query` (`:545-668`) to select from `canonical_passages passage` with `passage.search_vector @@ plainto_tsquery('simple', %s)` **but** with the identifier phrase requirement that distinguishes it from the lexical arm: use `websearch_to_tsquery`/`phraseto_tsquery('simple', token)` per identifier token (from `IDENTIFIER_TOKEN_RE`, `:31`) AND-ed, so it stays "exact identifiers present" rather than a second prose arm. The SELECT list becomes the same projection as `_lexical_query` (passage_id, ordinal, spans, receipts, `passage_first/last_occurred_at`) so `collapse_document_candidates` gets `passage_id` range keys (`:140-144`). Drop the `canonical_documents/events` joins; keep the `canonical_evidence_documents` + actor EXISTS. Keep `SPARSE_ARM_BUDGET_FRACTION` gating unchanged.
- Update the docstring block `:25-30` (no longer "every record, tool output included"). Note the known loss: identifiers that only occur in tool output (not visible user/assistant text) are no longer matched by search; they remain reachable via `recall_scan` records parquet. Record this in diagnostics as `"sparse_source": "passages"`.

EXIT CHECK:
- Unit (`tests/central_brain/test_canonical_retrieval.py`, `PassageArmTests`): update `test_sparse_arm_runs_only_for_identifier_shaped_queries` (`:1551,1554` assert `FROM canonical_passages passage` and no `FROM canonical_chunks chunk`); new `test_sparse_arm_never_reads_canonical_chunks`, `test_search_diagnostics_report_candidate_depth`, `test_search_limit_fifty_keeps_candidate_limit_bounded`.
- MCP conformance (`tests/central_brain/test_mcp_conformance.py`): `limit: 50` accepted, `51` rejected.
- Systems card: `accuracy.py` `CANDIDATE_LIMIT = 50`; `boundary_recall@50` becomes real. Gate unchanged this step; record `candidate_depth` metric.

### H2-b Convex min-max fusion (RRF kept behind a flag)

Files:
- `passage_retrieval.py`: new module-level `FUSION_MODES = ("convex", "rrf")`, `DEFAULT_FUSION_ALPHA = {"dense": a, "passage-lexical": b, "sparse-exact": c}` (sum to 1). Add `fusion: str` and `alphas: dict[str,float]` kwargs to `collapse_document_candidates` (`:106`). Implementation: per leg, take the best passage score per document, min-max normalise within the leg (`(s-min)/(max-min)`, degenerate → all 1.0 when the leg is a recent-first fallback with `0.0::real` scores — treat as rank-based fallback: `1/(60+rank)` normalised, so recency legs still contribute), then `doc_score = Σ alpha_leg * norm_leg`. Documents absent from a leg contribute 0 for that leg. Tie-break stays `(score, last_occurred_at, logical_document_id)`.
- `search()` (`:1049-1054`): read `self.store.fusion_mode`/`fusion_alphas` (set in `db.py:122-171` `BrainStore.__init__` from env `RECALL_SEARCH_FUSION=convex|rrf`, `RECALL_SEARCH_FUSION_ALPHAS=dense:0.25,lexical:0.35,sparse:0.40`). Emit `diagnostics["fusion"] = {"mode":..., "alphas":...}`.
- `fuse_document_rankings` (`:221`, bundle fusion across query expansions) stays RRF; it fuses whole rankings, not arms.
- Tuning script `evals/fusion_tuning.py` (new): loads the private truth set through `evals/private_holdout._load_jsonl/_private_path`, calls `PassageHintRetrieval.search(..., include_arms=True)` (already returns per-arm rankings at `:1082-1093`) once per case, caches arm rows in memory, then grid-searches α on a simplex (step 0.05) maximising MRR subject to recall@20 not decreasing, on the *train* split; reports validation split. Output: content-free JSON (α, aggregate metrics per split, truth_sha256, run id) written via the private-path rules (mode 0600, outside repo) — same shape as `evals/runner.py score --private --aggregate-only`.

EXIT CHECK:
- Unit: `test_convex_fusion_normalises_each_arm_before_weighting`, `test_convex_fusion_treats_recent_first_scores_as_rank_fallback`, `test_rrf_fallback_flag_restores_prior_ordering` (feed fixed legs; assert order), `test_fusion_alphas_env_parsing_rejects_non_simplex` (`tests/central_brain/test_brainstore_unit.py`).
- `tests/test_fusion_tuning.py`: script rejects in-repo truth path; report contains only aggregate fields (mirror `test_private_report_contains_only_aggregate_fields`).
- Systems card: validation MRR ≥ baseline+0.05 with recall@20 not lower; `diagnostics.fusion.mode == "convex"` recorded by the first-search diagnostics capture (#487).

### H2-c Reranker stage

Files:
- New `server/recall_server/rerank.py`: `class RerankRuntime` modelled on `SemanticRuntime.__init__` (`semantic.py:56-232`): `from_env()` reading `RECALL_RERANK_URL`, `RECALL_RERANK_APPROVED_URL`, `RECALL_RERANK_PROTOCOL=voyage|cohere`, `RECALL_RERANK_MODEL` (`rerank-2.5` / `rerank-4`), `RECALL_RERANK_KEY_FILE` | `RECALL_RERANK_KEY_ENV`, `RECALL_RERANK_TIMEOUT_MS` (default 600), `RECALL_RERANK_TOP_K` (50), `RECALL_RERANK_MIN_BUDGET_MS` (default 400). Reuse `_RejectRedirect`, `_read_owner_only_key`, `_read_json`/`MAX_SEMANTIC_RESPONSE_BYTES` by importing from `semantic.py` (or lift them into a small `_http.py`). Method `rerank(query, documents: list[str], *, deadline_at) -> list[tuple[int, float]]` with a `transport` injection point for tests (same pattern as `deep_inspection.HttpTransport`, `:209`). `fingerprint` property (protocol, model) for diagnostics.
- `passage_retrieval.py` `search()`: after arms complete and before `collapse_document_candidates`, if `self.store.rerank_runtime` and `deadline_at - now >= min_budget`: build the passage pool = union of all arm rows deduped by `passage_id` (fallback `receipt`), take the top 50 by fused document score (compute fusion first on the full pool, then take passages belonging to top-N documents up to 50), send `text_redacted` truncated to 2,000 chars (reranker input budget) with the header when H2-a lands, receive scores, then re-collapse: document score = max reranked passage score, ranges ordered by rerank score. Skip (status `skipped-deadline`, `skipped-disabled`, `unavailable`) on any error; never lose partial results. Add `diagnostics["rerank_status"]`, `rerank_elapsed_ms`, `rerank_candidates`, `rerank_model`. Put `rerank_elapsed_ms` also inside `arm_elapsed_ms` so `latency.search_stages` picks it up automatically (`latency.py:212-217`).
- Sequencing vs time clip: rerank runs inside `PassageHintRetrieval.search` so it precedes `_clip_passage_hints_to_time_window` (`canonical_retrieval.py:1825`), as required.
- Wiring: `db.py:171` add `self.rerank_runtime`; `app.py:1827` and `cli.py:1971` pass `RerankRuntime.from_env()`.
- Scoring: `evals/agentic_truth.py` add `boundary_recall@5`, `boundary_recall@10` next to `@20/@50` (`:675-684`, `_aggregate` `:508-517`). `accuracy.py` metrics/gates pick them up.

EXIT CHECK:
- Unit `tests/central_brain/test_rerank_runtime.py` with a fake transport: `test_rerank_requires_https_and_exact_approved_url`, `test_rerank_key_file_must_be_owner_only`, `test_rerank_rejects_redirects_and_oversized_responses`, `test_rerank_voyage_and_cohere_payload_shapes`, `test_rerank_returns_indices_in_provider_order_and_rejects_out_of_range`.
- `test_canonical_retrieval.py`: `test_rerank_reorders_top_fifty_passages_before_collapse`, `test_rerank_skipped_when_remaining_budget_below_minimum`, `test_rerank_failure_preserves_fused_results`, `test_rerank_elapsed_reported_in_arm_elapsed_ms`.
- `tests/test_agentic_truth.py`: `test_scores_recall_at_five_and_ten`.
- Systems card: recall@20 unchanged (±0.02), MRR ≥ 0.5, recall@5 ≥ 0.6 on validation; `arm.rerank.p95_ms` ≤ 400.

### H2-a Contextual header per passage (deterministic, no LLM)

Design: the header is *embedding input only*. `text_redacted`, `spans`, `receipts`, `text_sha256` are untouched, so `reconstruct_passage` (`passage_projection.py:291`) and receipts stay exact.

Files:
- `passage_projection.py`: add `render_passage_header(context: DocumentContext, *, first_occurred_at, last_occurred_at, roles) -> str` that calls `passage_representations._metadata_lines` and wraps as `"[context]\n" + lines + "\n[passage]\n"`; constant `PASSAGE_HEADER_CONTRACT = "recall.passage-header.v1:catalog-fields"`. Template order: source family, aliases, harness, workspace basename, branch, people[relations], passage start/end (use passage times, not document times — turn-level signal). Cap 512 bytes.
- Migration `server/schema/058_passage_embedding_headers.sql`: `ALTER TABLE canonical_passages ADD COLUMN header_redacted text, ADD COLUMN embed_sha256 char(64)`; backfill queue is the existing `canonical_passage_projection_queue`? No — re-projection reads S3. Instead add a small `passage_header_backfill` cursor table (tenant, source, last logical_document_id) so a worker computes headers from catalog only.
- `passage_index.py`: (1) `_commit` (`:444-479`) computes header via one catalog query per document (the non-support part of `_context_rows` `:463-547`: profile, aliases, session, actors) and writes `header_redacted`, `embed_sha256 = sha256(header + text)`; reuse join `:516-517` becomes `cached.content_sha256 = passage.embed_sha256`. (2) `embed_pending` (`:739-782`): select `header_redacted`, embed `header + "\n" + text_redacted`, condition `embedding.content_sha256 = passage.embed_sha256`, insert `content_sha256 = embed_sha256`. (3) New `backfill_headers(tenant_id, batch)` that fills `header_redacted/embed_sha256` for rows where NULL, called from `projection_worker.py` before `embed_pending`.
- Versioning: `semantic.py:29` `PASSAGE_EMBEDDING_CONTRACT` → `"recall.passage-embedding.v2:header+full-text"` and include `PASSAGE_HEADER_CONTRACT` in `passage_fingerprint` (`:312-324`). This changes `runtime.passage_fingerprint`, which is exactly the key the dense arm filters on (`passage_retrieval.py:816,878`) and the key `embed_pending` uses to find missing vectors. Rollout: env `RECALL_EMBEDDING_HEADER_CONTRACT=none|v1` selects which contract the runtime reports; worker runs with `v1` (writes new vectors under the new fingerprint; `ON CONFLICT ... DO UPDATE` at `:791-795` replaces the row), server stays on `none` until coverage from `CanonicalPassageRepresentationIndex.coverage`-style query (`passage_representations.py:794`) reports ≥ 99%, then flip the server. One deliberate re-embed; nothing else changes.
- Query side: prepend nothing to queries (Voyage asymmetric `input_type` already used, `semantic.py:665-678`).
- Cost: one Voyage-4 pass over all passages. Rough size: with `target_tokens=1024`, if the passage table is ~N passages, cost ≈ N × ~1,100 tokens × Voyage-4 price; e.g. 2 M passages ≈ 2.2 B tokens. Check actual `count(*) FROM canonical_passages` before scheduling; run with `RECALL_EMBEDDING_WORKERS` sharding (`embed_pending` `shard_count`, `:698`).

EXIT CHECK:
- Unit `tests/test_passage_projection.py`: `test_header_is_deterministic_and_excluded_from_spans_and_text_sha`, `test_header_is_bounded_to_512_bytes`, `test_embed_sha_changes_when_header_changes_but_passage_id_does_not`.
- `test_semantic_runtime.py`: `test_passage_fingerprint_includes_header_contract`.
- `server/tests/e2e_lossless_passages.py`: assert `header_redacted IS NOT NULL` for all passages, `count(embeddings) == count(passages)` after flip (`:341`), and reprojection reuses vectors when only actor names change? (No — header changes → re-embed; assert that instead.)
- Systems card: validation recall@20 and MRR do not regress; expected gain shows mostly in strata with person/repo/time intent (`stratum.*` metrics in `accuracy.py:126-129`).

### H2-e Systems card gates

- `accuracy.py:132-137`: `boundary_recall@20 ≥ 0.75`, `boundary_mrr ≥ 0.5`, add `boundary_recall@5 ≥ 0.6` (reported first, gated after one green run). `latency.py:134` `recall_search.p95_ms ≤ 1000` for warm calls (the tools probe already keeps the first call separate); `latency.py:225` `server_p95_ms ≤ 1000`.
- Card records `fusion.mode/alphas`, `rerank_model`, `candidate_depth`, `passage_fingerprint` from first-search diagnostics (#487 capture) so a green card is tied to a configuration.
- `evals/fusion_tuning.py` report path documented in `evals/README.md`.

---

## HILL 3 — S3 search plane (Lance)

### H3-a Outbox

Reuse `canonical_parquet_scan_queue` semantics but a separate table so Parquet and Lance cadences do not couple: migration `059_search_projection_outbox.sql`:

```
search_projection_outbox(tenant_id, source_id, bucket_start date, generation bigint, reason in ('backfill','logical-update','forget','header-change'), changed_at, PRIMARY KEY(tenant_id,source_id,bucket_start))
search_projection_tombstones(tenant_id, source_id, passage_id, tombstoned_at, generation)  -- explicit per-passage tombstones
search_projection_shards(tenant_id, source_id, bucket_start, dataset_uri, generation_sha256, row_count, fragment_count, vector_index_version, fts_index_version, embedding_fingerprint, created_at, compacted_at)
```

Writers: `passage_index._commit` (`:549-574`) inserts into the outbox alongside the parquet queue; `canonical.forget` (`canonical.py:1584-1626`) inserts tombstones for passages whose receipts intersect the forgotten native_ids (a query over `canonical_passages.receipts` with `&&`), then an outbox row `reason='forget'`. Seed backfill copies `canonical_parquet_scan_shards` months (same trick as `053_parquet_passage_plane.sql`).

EXIT: unit `test_search_outbox.py`: passage commit enqueues every month the document spans; forget writes tombstones and an outbox row; generation increments on conflict.

### H3-b Lance writer worker

New `server/recall_server/lance_projection.py` (`CanonicalLanceProjector`), scheduled in `projection_worker.py:20` after `scan`. Add `pylance` (the `lance` package) to `server/requirements.txt`; Dockerfile unchanged otherwise.

- Dataset per tenant per month: `s3://<bucket>/<namespace>/lance/<tenant_hash>/<YYYY-MM>/passages.lance` (S3 keys derived like `archive.py` namespace keys; same `RECALL_EVIDENCE_ARCHIVE_*` credentials, new `RECALL_SEARCH_PLANE_PREFIX`). Per-tenant-month (not per-source) so one hybrid query touches ≤ 12–24 datasets for a year window; source filter is a column predicate.
- Schema = parquet passages schema (`parquet_scan.py:240-259`) + `header string`, `spans string (json)`, `content_sha256 string`, `embedding_fingerprint string`, `vector fixed_size_list<float16, 512>`, `deleted bool`, `generation int64`. Vectors come straight from `canonical_passage_embeddings.embedding` (halfvec → float16 lossless).
- Write path per outbox row: stream passages for the month (query at `parquet_scan.py:589-618` plus embeddings join), `lance.write_dataset(..., mode="append")` as new fragments; apply tombstones and superseded revisions with `dataset.delete("passage_id IN (...)")` (Lance deletes are deletion vectors; cheap). Content-hash reuse: skip rows whose `(passage_id, content_sha256, embedding_fingerprint)` already exist (`dataset.to_table(columns=[...], filter=...)` on the month).
- Indexes: `create_index(column="vector", index_type="IVF_PQ", num_partitions≈sqrt(rows), num_sub_vectors=64, metric="cosine")`; `create_scalar_index("text", "INVERTED")` for BM25; scalar btree on `source_id`, `first_occurred_at`. Re-index when unindexed rows > 10% of month.
- Cadence: append every cycle; `optimize.compact_files()` + `cleanup_old_versions(older_than=7d)` nightly per month via a `RECALL_LANCE_COMPACT_HOUR` knob; only the writer process holds the (advisory-locked, `pg_try_advisory_lock` like `passage_index.py:730`) writer role per tenant-month.

EXIT: `server/tests/e2e_lance_projection.py` against MinIO (the e2e suites already use S3-shaped backends): project one month → dataset readable; re-run → zero new fragments (hash reuse); tombstone → row absent from `to_table(filter="deleted = false")`; compaction reduces fragment count. Unit `test_lance_projection.py`: schema equality with Arrow schema, float16 round-trip of a halfvec string.

### H3-c Where the query runs — recommendation: (i) in the MCP process via `lance` python with a local read cache

Reasons: (ii) the Archil sandbox costs seconds of stage/mount per call (`latency.archil_phases`), fine for `recall_scan`, not for a <1 s search; (iii) a query service is a second deployable to secure with the same key/URL hygiene and adds a hop. In-process `lance` reads S3 with byte-range requests, keeps an in-memory/disk block cache (`RECALL_LANCE_CACHE_DIR`, size cap), and the MCP server is already the process holding tenant authorization. Cold p95 is the risk; mitigate with a warm-on-boot of the last 3 months per active tenant and a background touch every 10 min.

Composition, in a new `server/recall_server/lance_retrieval.py` `LanceHintRetrieval` with the same `search(query, lexical_query, since, until, limit, deadline_at)` signature as `PassageHintRetrieval` (`passage_retrieval.py:981`):
1. Catalog pre-filter from Postgres (unchanged code): authorized `sources` (`canonical_retrieval._sources`), actor ids (`:1778-1795`), time window → months to open.
2. Per month, concurrently: `ds.scanner(nearest={"column":"vector","q":qvec,"k":400}, filter="source_id IN (...) AND deleted=false AND last_occurred_at >= ... ")` and `ds.scanner(full_text_query=lexical_query, filter=...)`; exact-identifier arm = FTS phrase query on identifier tokens (same gating `sparse_arm_applies`). Actor filter as `array_has_any(actor_ids, [...])`.
3. Rows are mapped into the same row dicts the legs use today (`logical_document_id, revision, native_parent_id, first/last_occurred_at, manifest_object_key, manifest_content_sha256, passage_id, passage_ordinal, spans, receipts, text_redacted, score`), fetching the document-level fields from `canonical_evidence_documents` by id in one query. Then the *same* `collapse_document_candidates` (convex), `RerankRuntime`, and `_clip_passage_hints_to_time_window`. Response contract identical; `diagnostics.engine = "lossless-passages-lance-v1"`, plus `months_opened`, `cache_hit_rate`.
4. Selection in `canonical_retrieval.passage_hints` (`:1807`) by `self.store.search_plane in {"postgres","lance","shadow"}` (`RECALL_SEARCH_PLANE`), `shadow` runs both and logs a content-free comparison (rank agreement, elapsed) to `search_plane_shadow_log`.

EXIT: `tests/central_brain/test_lance_retrieval.py` with an in-memory Lance dataset on a temp dir: same query returns the same `results` shape as `PassageHintRetrieval` (assert keys of results/matching_ranges equal), tombstoned passage absent, actor/source/time filters applied as predicates, deadline honoured (skip months when over budget). `test_mcp_conformance.py`: `recall_search` output schema unchanged.

### H3-d Postgres becomes a catalog

Drop order (each behind the cutover flag having been `lance` for ≥ 14 days with green card):
1. `canonical_passage_embeddings` (HNSW) — after Lance vector arm passes shadow gate. Migration `060_drop_passage_embeddings.sql`.
2. `canonical_passages.search_vector` + GIN `canonical_passages_search_idx`; `text_redacted` moved to Lance (keep `text_sha256`, `embed_sha256`, `receipts`, `spans`, times, roles, actors). `061`.
3. Representation tables `canonical_passage_contexts`, `canonical_passage_embedding_representations` (`042/043`) and the `search_representation` code path (`passage_retrieval.py:1179`). `062`.
4. `canonical_chunks` bodies + `search_vector` GIN, after H3-e; keep `(tenant, source, document_id, chunk_id, ordinal, receipt, deleted_at)` as the receipt map (`063`, table rename to `canonical_receipts`). Legacy plane (`_legacy_chunk_search_for_eval`, `_entity_leg` in `db.py`) removed in `064`.
Stays: `canonical_evidence_documents`, `canonical_passage_documents`, `canonical_passages` (ids/times/roles/receipts/spans/actor links), `canonical_events`/`canonical_documents` metadata, actors, grants, queues/outbox, parquet/lance shard catalogs.
Dual-write period: H3-b writes Lance while Postgres keeps writing (already true); shadow-read via `RECALL_SEARCH_PLANE=shadow`; cutover `lance`; rollback is flipping back until step 1 drops.

EXIT: `e2e_storage_legacy_cut.py` extended: after `064`, the MCP conformance suite passes; `SELECT pg_database_size()` < 10 GB on the prod replica (card `cost.planetscale` storage metric, gate added).

### H3-e show / session_context / related from S3 evidence parts + catalog

- `_receipt_event` (`canonical_retrieval.py:2879-2907`) → resolve via the receipt map + `canonical_events` (no chunk text). `show` (`:4012-4019`) → read the logical part containing the record: `canonical_evidence_documents`/parts catalog gives `manifest_object_key`; `logical_evidence.read_part` (`logical_evidence.py:1067`) fetches bytes; decode with `decode_logical_record` (`passage_projection.py:452`) and slice records by receipt. Add a small LRU part cache (`RECALL_EVIDENCE_PART_CACHE_MB`). `session_context` (`:2969-3044`) → same part, neighbours by ordinal. `related` (`:4055-4086`) → `canonical_events.canonical_redacted #>> provenance` already in Postgres; text comes from Lance (`passages` month dataset filtered by `logical_document_id`) or the part.
- Time clip (`:1887-1919`) → `canonical_events.occurred_at` by receipt via the receipt map; no text needed (it only reads `text_redacted` to re-emit; drop it).

EXIT: `latency.tools` `recall_show.p95_ms ≤ 2000` unchanged; `test_canonical_retrieval.py` `TimeClipWindowTests` updated to assert no `canonical_chunks` in SQL; `e2e_logical_evidence_projection.py` asserts `show` returns byte-identical `text` before/after cutover for a fixed receipt.

### H3-f Exit checks for the hill

- Shadow comparison: run `evals/systems_card` twice with `RECALL_SEARCH_PLANE=postgres` and `=lance` on the validation split; gate Lance recall@20 ≥ Postgres and MRR ≥ Postgres − 0.02; store in `history.jsonl` with `engine`.
- Cold p95 < 2 s: new `latency.search_cold` probe that restarts the cache (`RECALL_LANCE_CACHE_DIR` cleared via an admin endpoint) then measures 10 queries; warm p95 < 1 s unchanged.
- Postgres < 10 GB: `cost.planetscale` storage gate.
- Cost per seat: card `cost` dimension adds S3 GB-month + request count from the shard catalog and Voyage/rerank token estimates from diagnostics counters; report, gate later.
- Forget e2e: `server/tests/e2e_forget_lance.py`: forget a receipt → passage tombstone row → outbox `forget` → worker cycle → `recall_search` for a unique phrase from that passage returns nothing within N = 2 worker intervals; also `show` on the receipt returns null.

---

## Risks and verification

- Sparse arm on passages loses tool-output identifiers (H2-d). Verify with the truth set stratum for identifier queries before/after; if recall drops, keep a chunk-backed sparse arm gated to `since/until` scoped queries only.
- Min-max with tiny legs (1–2 candidates) degenerates (H2-b). Guard: legs with < 3 candidates use rank fallback; unit test covers it.
- Reranker latency eats the 5 s budget on slow days (H2-c). `RECALL_RERANK_MIN_BUDGET_MS` skip path, diagnostics show skip rate; card gate `rerank_skipped_rate ≤ 0.1`.
- Header re-embed cost and mixed-fingerprint window (H2-a). Two-fingerprint rollout above; `coverage` query in the card freshness probe.
- Lance cold reads from S3 (H3-c). Warm-on-boot + cache; if cold p95 fails, fall back to option (iii) later — the `LanceHintRetrieval` boundary keeps this swappable.
- Lance index staleness after appends (H3-b). Unindexed rows are still scanned flat by Lance; threshold re-index; card metric `lance.unindexed_rows`.
- Deleting text from Postgres before every reader moved (H3-d). Grep gate in CI: `tests/central_brain/test_storage_cut.py` asserts no `text_redacted` read outside the allowed modules after `061/063`.

Rough sizes: H2-d ~300 lines; H2-b ~250 + script ~200; H2-c ~450 + tests; H2-a ~350 + migration; H3-a ~150; H3-b ~700; H3-c ~600; H3-d 5 migrations + ~400 deletions; H3-e ~400.

### Critical Files for Implementation
- /home/ubuntu/worktrees/recall-rewrite-20260911/recall/server/recall_server/passage_retrieval.py
- /home/ubuntu/worktrees/recall-rewrite-20260911/recall/server/recall_server/passage_index.py
- /home/ubuntu/worktrees/recall-rewrite-20260911/recall/server/recall_server/canonical_retrieval.py
- /home/ubuntu/worktrees/recall-rewrite-20260911/recall/server/recall_server/semantic.py
- /home/ubuntu/worktrees/recall-rewrite-20260911/recall/server/recall_server/parquet_scan.py
- /home/ubuntu/worktrees/recall-rewrite-20260911/recall/evals/systems_card/accuracy.py