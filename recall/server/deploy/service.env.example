RECALL_DATABASE_URL=postgresql://recall:replace-me@127.0.0.1:55434/recall
RECALL_AUTH_REQUIRED=1
RECALL_TRUST_TAILSCALE_HEADERS=1
RECALL_TRUSTED_PROXY_UIDS=0,65534
RECALL_ALLOWED_TAILSCALE_USERS=owner@example.com
# Private raw archive. Keep the bucket private and scope the S3 credential to it.
# RECALL_ARCHIVE_BACKEND=r2
# RECALL_ARCHIVE_BUCKET=recall-raw-owner
# RECALL_ARCHIVE_ENDPOINT_URL=https://ACCOUNT_ID.r2.cloudflarestorage.com
# RECALL_ARCHIVE_REGION=auto
# RECALL_ARCHIVE_ACCESS_KEY_ID is injected by the deployment secret manager.
# RECALL_ARCHIVE_SECRET_ACCESS_KEY is injected by the deployment secret manager.
# RECALL_ARCHIVE_NAMESPACE_KEY is an independent base64-encoded 32-byte secret.
# Cloudflare management API tokens are neither required nor read by Recall.
# Deep inspection reads a separate, privacy-processed evidence projection. Never
# point this configuration or an Archil disk at the raw archive above. Deploy one
# evidence archive and one Archil disk per exact brain tenant.
# RECALL_EVIDENCE_ENABLED=1
# RECALL_EVIDENCE_TENANT_ID=tenant:company:example
# RECALL_EVIDENCE_ARCHIVE_BACKEND=r2
# RECALL_EVIDENCE_ARCHIVE_BUCKET=recall-evidence-company-example
# RECALL_EVIDENCE_ARCHIVE_ENDPOINT_URL=https://ACCOUNT_ID.r2.cloudflarestorage.com
# RECALL_EVIDENCE_ARCHIVE_REGION=auto
# RECALL_EVIDENCE_ARCHIVE_ACCESS_KEY_ID is injected by the deployment secret manager.
# RECALL_EVIDENCE_ARCHIVE_SECRET_ACCESS_KEY is injected by the deployment secret manager.
# RECALL_EVIDENCE_ARCHIVE_NAMESPACE_KEY is an independent base64-encoded 32-byte secret.
# RECALL_DEEP_INSPECTOR=archil
# RECALL_DEEP_INSPECTION_REQUIRED=1
# ARCHIL_API_KEY is injected by the deployment secret manager.
# RECALL_ARCHIL_DISK_ID=dsk_replace_me
# RECALL_ARCHIL_REGION=aws-us-west-2
# AWS S3 alternative (use `s3` when bucket versioning is enabled):
# RECALL_EVIDENCE_ARCHIVE_BACKEND=s3-unversioned
# RECALL_EVIDENCE_ARCHIVE_BUCKET=recall-evidence-example
# RECALL_EVIDENCE_ARCHIVE_ENDPOINT_URL=https://s3.us-west-2.amazonaws.com
# RECALL_EVIDENCE_ARCHIVE_REGION=us-west-2
# Canonical v2 cutover. Keep disabled until archive-check and migrations pass.
# RECALL_CANONICAL_V2_ENABLED=1
# RECALL_CANONICAL_INGEST_PUBLIC=1
# Route authenticated MCP reads exclusively through canonical tenant/source grants.
# RECALL_CANONICAL_MCP_ENABLED=1
# RECALL_TENANT_ID=tenant:personal
# RECALL_PRINCIPAL_ID=principal:owner
# Unified web/native connector administration. Keep disabled until schema 029,
# an independent encryption key, and an exact provider redirect are configured.
# RECALL_ADMIN_WEB_ENABLED=1
# RECALL_CONTROL_ENCRYPTION_KEY is injected by the deployment secret manager.
# Google is optional, but the three Google values are all-or-none.
# RECALL_GOOGLE_CLIENT_ID is injected by the deployment secret manager.
# RECALL_GOOGLE_CLIENT_SECRET is injected by the deployment secret manager.
# RECALL_GOOGLE_REDIRECT_URI=https://recall.example/admin/oauth/callback/google
# Slack is optional, but all four values are all-or-none.
# RECALL_SLACK_CLIENT_ID is injected by the deployment secret manager.
# RECALL_SLACK_CLIENT_SECRET is injected by the deployment secret manager.
# RECALL_SLACK_SIGNING_SECRET is injected by the deployment secret manager.
# RECALL_SLACK_REDIRECT_URI=https://recall.example/admin/oauth/callback/slack
# Optional hosted auth/transport rail. Use a scoped Composio project key with
# connected-account read and proxy-execute permissions; never a browser key.
# RECALL_COMPOSIO_API_KEY is injected by the deployment secret manager.
# RECALL_COMPOSIO_REDIRECT_URI=https://recall.example/admin/oauth/callback/composio
# Optional custom auth configs. Omit to use Composio-managed development auth.
# RECALL_COMPOSIO_AUTH_CONFIG_GMAIL=ac_replace_me
# RECALL_COMPOSIO_AUTH_CONFIG_GOOGLECALENDAR=ac_replace_me
# RECALL_COMPOSIO_AUTH_CONFIG_GOOGLECONTACTS=ac_replace_me
# RECALL_COMPOSIO_AUTH_CONFIG_GOOGLEDRIVE=ac_replace_me
# Comma-separated exact HTTPS hosts allowed for expiring document-export downloads.
# Keep empty to disable document body export through Composio.
# RECALL_COMPOSIO_BINARY_HOSTS=files.example.invalid
# Let the managed worker also drain full logical documents, lossless passages,
# and their embeddings through the configured private evidence archive.
# RECALL_MANAGED_PROJECTIONS_ENABLED=1
# Browser MCP clients must match this exact comma-separated allow-list.
# Non-browser agent clients normally omit Origin.
# RECALL_MCP_ALLOWED_ORIGINS=https://approved-agent.example
# RECALL_MCP_RESOURCE_URI=https://recall.example.com/mcp
# RECALL_AUTHORIZATION_SERVERS=https://identity.example.com
# Standards-based human MCP access. `descope` is a convenience preset over the same OIDC verifier.
# RECALL_MCP_AUTH_PROVIDER=descope
# RECALL_OIDC_ISSUER=https://api.descope.com/v1/apps/agentic/<project>/<mcp-server>
# Optional exact token issuers that share the same trust domain and subject IDs.
# They are verified normally, then mapped to RECALL_OIDC_ISSUER for one identity.
# RECALL_OIDC_ADDITIONAL_ISSUERS=https://api.descope.com/v1/apps/agentic/<project>/<mcp-server>
# RECALL_OIDC_JWKS_URI=https://api.descope.com/<project>/.well-known/jwks.json
# Optional OAuth-first browser identity. Configure all three or none. The client
# secret stays in the deployment secret manager; provider tokens are not stored.
# RECALL_IDENTITY_OAUTH_CLIENT_ID=<confidential Inbound App client ID>
# RECALL_IDENTITY_OAUTH_CLIENT_SECRET is injected by the deployment secret manager.
# RECALL_IDENTITY_OAUTH_REDIRECT_URI=https://recall.example/admin/oauth/callback/identity
# Initial claim is allowed only for these verified emails and only while the
# deployment has exactly one unclaimed owner principal.
# RECALL_BOOTSTRAP_OWNER_EMAILS=owner@example.com
# Optional company invitation email. Disabled unless the provider is explicit.
# Descope uses its configured user-invitation template and links to Recall's
# brain-specific setup page. Keep the management key server-side.
# RECALL_INVITATION_EMAIL_PROVIDER=descope
# RECALL_DESCOPE_PROJECT_ID=P_replace_me
# RECALL_DESCOPE_MGMT_KEY is injected by the deployment secret manager.
# Optional static public OAuth client for Codex installations when the identity
# provider does not support dynamic client registration. Configure both or neither.
# RECALL_CODEX_OAUTH_CLIENT_ID=<public OAuth client ID>
# RECALL_CODEX_OAUTH_CALLBACK_PORT=8765
# Provider-neutral transactional email alternative:
# RECALL_INVITATION_EMAIL_PROVIDER=resend
# RECALL_INVITATION_EMAIL_FROM=Recall <recall@example.com>
# RECALL_INVITATION_EMAIL_API_KEY is injected by the deployment secret manager.
# Semantic retrieval is opt-in and fail-closed. Choose one profile from deploy/README.md.
# Managed Voyage example (recommended hosted profile):
# RECALL_EMBEDDING_PROTOCOL=voyage
# RECALL_EMBEDDING_URL=https://api.voyageai.com
# RECALL_EMBEDDING_APPROVED_URL=https://api.voyageai.com
# RECALL_EMBEDDING_KEY_ENV=VOYAGE_API_KEY
# VOYAGE_API_KEY is injected by the deployment secret manager; never commit it here.
# RECALL_EMBEDDING_MODEL=voyage-4
# RECALL_EMBEDDING_REVISION=voyage-4
# RECALL_EMBEDDING_DIMENSIONS=512
# RECALL_EMBEDDING_BATCH_SIZE=64
# Managed embedding workers may opt into 2-8 parallel document batches.
# RECALL_EMBEDDING_WORKERS=1
# Calibrate this against a private retrieval eval when changing embedding models.
# RECALL_SEMANTIC_MINIMUM_SIMILARITY=0.35
# Passage-search arm fusion (H2-b). convex: min-max normalise each arm's best
# document score, then sum alpha_arm * normalised (legs with < 3 documents or
# recent-first 0.0 scores fall back to rank scores). rrf: prior reciprocal-rank
# fusion with the 0.15/0.30/0.55 leg weights. Alphas must sum to 1; tune them
# offline with `python -m evals.fusion_tuning` (see recall/evals/README.md).
# RECALL_SEARCH_FUSION=convex           # convex | rrf
# RECALL_SEARCH_FUSION_ALPHAS=dense:0.15,lexical:0.30,sparse:0.55
# Query-time temporal hints (H2-h). A date phrase in the question ("around May
# 2-4", "last week", "Q2", "2026-05-03") becomes a soft window boost, never a
# filter: documents intersecting the window get their fused score x (1 + boost)
# before rerank and collapse, and a day-level hint (hedged or not) also runs one windowed
# dense pass (budgeted) whose pool is unioned with the global one. Explicit
# since/until filters bypass the parser.
# RECALL_TEMPORAL_HINTS=on               # on | off
# RECALL_TEMPORAL_BOOST=0.5              # exact hints (explicit dates, yesterday, last Tuesday)
# RECALL_TEMPORAL_BOOST_LOOSE=0.25       # months, quarters, relative and hedged phrases
# RECALL_TEMPORAL_WINDOW_BUDGET_MS=150   # budget of the extra windowed dense pass
# Optional cross-encoder reranker (H2-c). Off by default; any failure falls back to
# the fused ranking. Hosted providers receive the query plus up to
# RECALL_RERANK_MAX_CANDIDATES redacted passages truncated to RECALL_RERANK_MAX_DOC_CHARS.
# RECALL_RERANK_PROTOCOL=off            # voyage | cohere | off
# RECALL_RERANK_MODEL=rerank-2.5        # cohere default: rerank-v3.5
# RECALL_RERANK_URL=https://api.voyageai.com/v1/rerank
# RECALL_RERANK_APPROVED_URL=https://api.voyageai.com/v1/rerank
# RECALL_RERANK_KEY_ENV=VOYAGE_API_KEY  # or RECALL_RERANK_KEY_FILE=/etc/recall/rerank.key (0600)
# RECALL_RERANK_TIMEOUT_SECONDS=2.5
# RECALL_RERANK_MAX_CANDIDATES=50
# RECALL_RERANK_MAX_DOC_CHARS=2000
# RECALL_RERANK_MIN_BUDGET_SECONDS=1.0  # skip the reranker when less of the deadline remains
# Write-path tuning (deploy/README.md): identity registration cache and
# batched allowed-decision authorization audit. Denials always write synchronously.
# RECALL_IDENTITY_CACHE_TTL_SECONDS=600
# RECALL_IDENTITY_CACHE_MAX=10000
# RECALL_AUDIT_BATCH_ROWS=500
# RECALL_AUDIT_BATCH_SECONDS=2
# Generic OpenAI-compatible services use RECALL_EMBEDDING_PROTOCOL=openai. Models
# such as Nomic may also require explicit query/document prefixes:
# RECALL_EMBEDDING_DOCUMENT_PREFIX="search_document: "
# RECALL_EMBEDDING_QUERY_PREFIX="search_query: "
# Pinned TEI/Qwen remains available as the private self-hosted profile.
# RECALL_LITELLM_URL=https://staging-router.example.com
# RECALL_LITELLM_APPROVED_URL=https://staging-router.example.com
# RECALL_LITELLM_MODEL=gpt-5.4-mini
# RECALL_LITELLM_KEY_FILE=/home/owner/.config/recall-brain/planner.key
# RECALL_PLANNER_SAMPLES=2
# RECALL_SEMANTIC_TIMEOUT_SECONDS=30
# RECALL_SEMANTIC_CACHE_SIZE=256
# RECALL_SEMANTIC_CACHE_TTL_SECONDS=900
# Optional content-addressed DuckDB CLI for read-only Parquet scans in Archil.
# Publish a checksum-verified binary with `publish-archil-duckdb`; these two
# values are artifact identity, not credentials, and must be configured together.
# RECALL_ARCHIL_DUCKDB_OBJECT_KEY=objects/opaque/content-addressed-key
# RECALL_ARCHIL_DUCKDB_SHA256=64-lowercase-hex-characters
LOG_LEVEL=INFO
